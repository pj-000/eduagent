# 工作流设计报告

> 生成时间：2026-03-05 20:49:50
> 数据来源：custom_goal

## 工作流目标

Design a workflow to build an AI-powered adaptive quiz generator for K12 math education

## 子任务列表

### 1. requirements_synthesis

**描述**：Extract and formalize functional and non-functional requirements from the goal, including pedagogical constraints (e.g., grade bands, standards alignment), adaptation criteria (e.g., difficulty scaling, misconception targeting), and AI capabilities (e.g., explanation generation, distractor diversity).

**输入**：
- `goal` (string): The user's goal in textual format.

**输出**：
- `formal_requirements` (string): A structured list of functional requirements (e.g., 'Generate quizzes with 3–7 questions per session'), non-functional requirements (e.g., 'Adapt question difficulty within 2 seconds of submission'), and pedagogical constraints (e.g., 'All Grade 5 questions must align to Common Core Standard 5.NF.A.1').

**Agent Prompt**：

```
### Objective
Transform the user's goal into a precise, categorized, and educationally valid list of functional requirements, non-functional requirements, and pedagogical constraints — ensuring each item is concrete, measurable, and grounded in K12 math education practice.

### Instructions
1. Caref...
```

---

### 2. domain_modeling

**描述**：Define the core domain entities, attributes, and relationships required to support adaptive quiz generation, including StudentProfile, MathStandard, QuestionTemplate, ProficiencyScore, and QuizSession.

**输入**：
- `goal` (string): The user's goal in textual format.
- `formal_requirements` (string): The structured list of functional, non-functional, and pedagogical requirements.

**输出**：
- `domain_schema` (string): A JSON-like schema describing key entities (e.g., 'StudentProfile': { 'grade_level': 'int', 'current_standards': ['string'], 'recent_performance': [{'standard_id': 'string', 'accuracy': 'float'}] }), with relationships and constraints.

**Agent Prompt**：

```
### Objective
Construct a precise, pedagogically valid, and computationally actionable domain schema for an AI-powered adaptive quiz generator, identifying all core entities (StudentProfile, MathStandard, QuestionTemplate, ProficiencyScore, QuizSession), their attributes, data types, collections, ne...
```

---

### 3. adaptation_logic_design

**描述**：Specify the rules and triggers governing how the system adapts quizzes in response to student interactions, including difficulty adjustment heuristics, topic reweighting strategies, and scaffolding escalation paths.

**输入**：
- `goal` (string): The user's goal in textual format.
- `formal_requirements` (string): The structured list of functional, non-functional, and pedagogical requirements.
- `domain_schema` (string): The JSON-like schema of core domain entities and relationships.

**输出**：
- `adaptation_rules` (string): A set of if-then rules and state-transition definitions (e.g., 'IF StudentProfile.grade_level == 6 AND ProficiencyScore["6.EE.A.2"] < 0.6 THEN increase weight of scaffolded expression-building questions by 40%') governing quiz adaptation.

**Agent Prompt**：

```
### Objective
Design a set of precise, pedagogically grounded, and schema-compliant adaptation rules that govern how the quiz system responds to student interactions—including difficulty adjustment, topic reweighting, and scaffolding escalation.

### Instructions
1. Analyze the user's goal: <input>{...
```

---

### 4. ai_component_specification

**描述**：Decompose the 'AI-powered' layer into three modular, interface-defined components: (1) QuestionGenerator (input: MathStandard + difficulty; output: question + distractors + explanation), (2) ProficiencyInferenceEngine (input: QuizSession responses; output: updated ProficiencyScore per standard), and (3) QuizAssembler (input: StudentProfile + adaptation_rules + generated questions; output: ordered QuizSession).

**输入**：
- `goal` (string): The user's goal in textual format.
- `formal_requirements` (string): The structured list of functional, non-functional, and pedagogical requirements.
- `domain_schema` (string): The JSON-like schema of core domain entities and relationships.
- `adaptation_rules` (string): The set of if-then rules and state-transition definitions governing quiz adaptation.

**输出**：
- `ai_component_interfaces` (string): A precise interface specification for each of the three AI components, including input schemas, output schemas, expected latency, and failure-handling behavior (e.g., 'QuestionGenerator must return a valid JSON object with keys: "stem", "options", "correct_answer", "explanation" within 1.5s').

**Agent Prompt**：

```
### Objective
Generate a precise, production-ready interface specification for the QuestionGenerator AI component, grounded in the user goal, formal requirements, and domain schema.

### Instructions
1. Extract pedagogical and functional constraints relevant to question generation from <input>{goal}...
```

---

### 5. workflow_integration_and_validation

**描述**：Define the end-to-end workflow sequence, data flow dependencies, and validation strategy—including simulation-based testing (e.g., synthetic student profiles), alignment checks against curriculum standards, and metrics for adaptation efficacy (e.g., 'time-to-mastery reduction').

**输入**：
- `goal` (string): The user's goal in textual format.
- `formal_requirements` (string): The structured list of functional, non-functional, and pedagogical requirements.
- `domain_schema` (string): The JSON-like schema of core domain entities and relationships.
- `adaptation_rules` (string): The set of if-then rules and state-transition definitions governing quiz adaptation.
- `ai_component_interfaces` (string): The precise interface specification for each of the three AI components.

**输出**：
- `integrated_workflow_spec` (string): A complete, executable workflow specification: a numbered sequence of steps (e.g., '1. Receive student ID → 2. Load StudentProfile → 3. Query ProficiencyInferenceEngine → 4. Apply adaptation_rules → 5. Call QuestionGenerator N times → 6. Assemble via QuizAssembler → 7. Log QuizSession'), with explicit inputs/outputs per step, error-handling branches, and validation checkpoints.

**Agent Prompt**：

```
### Objective
Synthesize the user's goal, formal requirements, domain schema, adaptation rules, and AI component interfaces into a complete, executable, and validated end-to-end workflow specification for the adaptive quiz generator.

### Instructions
1. Parse <input>{goal}</input> to identify the c...
```

---
